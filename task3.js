let nums = [];

function uniqueTriplets(nums) {
    for (num in nums) {
        let x = Math.floor(Math.random(nums) * nums.length)
        let y = Math.floor(Math.random(nums) * nums.length)
    }
}

// PseudoCode
// for a given array of integers
// create a function that accepts a given array of integers,
// from the array, pick a random number say, x
// from the array, pick a random number say, y where y != x